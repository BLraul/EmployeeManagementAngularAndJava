export interface Employee {
    id?: number; //tip optional
    name?: string;
    email?: string;
    jobTitle?: string;
    phone?: string;
    imageUrl?: string;
    employeeCode?: string;
  }  
  /* aceasta reprezinta tipurile de date care vor fi retunate 
  ori de cate ori vom face apeluri spre back-end pentru ca doar 
  oglindim din punct de vedere tehnic cum arata un angajat din back-end*/